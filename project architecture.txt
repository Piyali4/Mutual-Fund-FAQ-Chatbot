Let’s convert your full plan into clean English step by step so you can directly build the project 👇

🚀 Project Setup (RAG Mutual Fund Chatbot)
🔹 Step 1: Product + AMC Selection

I suggest you use this combination (simple + clean data):

Platform: Groww
AMC: SBI Mutual Fund
📊 Schemes (3–5)
SBI Bluechip Fund (Large Cap)
SBI Flexicap Fund
SBI Long Term Equity Fund (ELSS)
SBI Small Cap Fund

👉 These are easily available on the official website.

🔹 Step 2: Data Collection (15–25 URLs)
📚 Collect these pages:
🏦 AMC Website
Scheme factsheet
KIM (Key Information Memorandum)
SID (Scheme Information Document)
🏛️ Official Sources
AMFI
SEBI

👉 Collect information like:

Expense ratio
Riskometer explanation
Exit load rules
ELSS lock-in rules
Statement download guide
📄 Example Source List
https://www.sbimf.com/en-us/mutual-fund/sbi-bluechip-fund
https://www.sbimf.com/en-us/factsheet
https://www.amfiindia.com/investor-corner
https://www.sebi.gov.in/sebi_data
🔹 Step 3: RAG Architecture (Simple)
User Question  
   ↓  
Retriever (FAISS / Chroma)  
   ↓  
Relevant chunks  
   ↓  
LLM (GPT)  
   ↓  
Answer + Source link  
🔹 Step 4: Tech Stack
Python
LangChain
OpenAI / Local LLM
FAISS (Vector Database)
Streamlit (UI)
🔹 Step 5: Code (Minimal Working Prototype)
📦 Install
pip install langchain openai faiss-cpu streamlit tiktoken
🧠 RAG Code (Python)
from langchain.document_loaders import WebBaseLoader
from langchain.text_splitter import RecursiveCharacterTextSplitter
from langchain.vectorstores import FAISS
from langchain.embeddings import OpenAIEmbeddings
from langchain.chat_models import ChatOpenAI
from langchain.chains import RetrievalQA

# 1. Load data
urls = [
    "https://www.sbimf.com/en-us/mutual-fund/sbi-bluechip-fund",
]

docs = []
for url in urls:
    loader = WebBaseLoader(url)
    docs.extend(loader.load())

# 2. Split
splitter = RecursiveCharacterTextSplitter(chunk_size=500, chunk_overlap=50)
chunks = splitter.split_documents(docs)

# 3. Embedding
embeddings = OpenAIEmbeddings()
db = FAISS.from_documents(chunks, embeddings)

# 4. LLM
llm = ChatOpenAI(temperature=0)

qa = RetrievalQA.from_chain_type(
    llm=llm,
    retriever=db.as_retriever()
)

# 5. Query
query = "What is expense ratio of SBI Bluechip Fund?"
response = qa.run(query)

print(response)
🔹 Step 6: Add RULES (Very Important ⚠️)

👉 Prompt control (facts-only)

SYSTEM_PROMPT = """
You are a Mutual Fund FAQ assistant.

Rules:
- Answer only factual questions
- Max 3 sentences
- Always include ONE source link
- No investment advice
- If opinion question → refuse politely

Refusal:
"I can only provide factual information, not investment advice. Please refer to official sources: <link>"
"""
🔹 Step 7: Streamlit UI
import streamlit as st

st.title("📊 Mutual Fund FAQ Bot")

st.write("Facts-only. No investment advice.")

st.write("Example questions:")
st.write("- What is expense ratio?")
st.write("- What is exit load?")
st.write("- ELSS lock-in period?")

query = st.text_input("Ask your question:")

if query:
    answer = qa.run(query)
    st.write(answer)
🔹 Step 8: Refusal Logic

👉 Detect opinion-based queries:

def is_opinion(q):
    keywords = ["should I", "best fund", "invest", "buy"]
    return any(k in q.lower() for k in keywords)

if is_opinion(query):
    st.write("I can only provide factual info, not advice.")
🔹 Step 9: Sample Q&A

Q: What is ELSS lock-in?
A: ELSS schemes have a lock-in period of 3 years.
Source: https://www.amfiindia.com

Q: Should I invest in SBI Bluechip?
A: I can only provide factual information, not investment advice. Please refer to official sources: https://www.amfiindia.com

🔹 Step 10: README (Short Template)

Project: Mutual Fund FAQ Chatbot

AMC: SBI Mutual Fund
Schemes: Bluechip, Flexicap, ELSS

Setup:
pip install -r requirements.txt
streamlit run app.py
Limits:
No advice
Small dataset
🔥 Final Tip

👉 The 3 most important things in this project:

Correct source link
Strict refusal logic
Short answers

